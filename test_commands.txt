Test commands list :

1)created new folder Git-Test and initialized to git.
-commands used:
    git status
    git init

2)created test_commands.txt, added in staging area and committed with message
-commands used:
    git status
    git add test_commands.txt
    git commit -m "editing and commiting this file"

3) created and visited branch : feature
-commands used:
    git branch
    git switch -c features
    git switch main

4) Push and pull code in github repository.
-commands used :
    git remote add origin "https://github.com/AyushMahera/Git-Test.git"
    git push origin main
    git switch features
    git pull origin main

(this lines are added in features branch)
5) merging branches
commands used:
    git branch
    git switch features
    git push features
    git switch main
    git merge 
    
6)Resolving conflicts
-this line is added in main branch